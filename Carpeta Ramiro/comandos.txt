git clone + url https 	//agrega la carpeta clonada al disco

git status //me muestra si hay archivos muevos

git add . //preparo todo para subir al servidor

git status 

git commit -m "mensaje del commit" -m "descripción del commit"	//uso commit para generar la versión para subir

git push	//sube todo al servidor

git pull	//baja todo del servidor